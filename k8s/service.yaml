apiVersion: v1
kind: Service
metadata:
  name: abyss-backend
  labels:
    app.kubernetes.io/name: abyss-backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: abyss-backend
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
